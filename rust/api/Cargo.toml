[package]
name = "api"
version = "0.1.0"
edition = "2021"
autolib = false

[dependencies]
actix-web = "4.9.0"
serde = { version = "1.0.218", features = ["derive"]}
serde_json = "1.0.139"

applications = { path = "../applications" }
domain = { path = "../domain" }

[patch.crates-io]
# Same as the root workspace
applications = { path = "../applications" }
domain = { path = "../domain" }

[lib]
path = "mod.rs" 
